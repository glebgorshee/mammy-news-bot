__pycache__/
*.pyc
.env
.venv/
venv/
.DS_Store

# Local runner with secrets — DO NOT COMMIT
run_bot.ps1

# Runtime logs
*.log
*.log.old
run.log.tmp.*

# state.json раньше был в .gitignore — но workflow коммитит его
# обратно в репо как «память бота», и без коммита каждый запуск
# стартует с нулевой памятью и рискует публиковать дубли.

# Symlink to Claude Code memory (separate repo)
CLAUDE-MEMORY
